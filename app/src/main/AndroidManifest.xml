<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lorenzomalferrari.holidaydiary">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Activity iniziale -->
        <activity
            android:name=".view.SplashActivity"
            android:theme="@style/AppThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login se l'utente non è loggato -->
        <activity android:name=".view.LoginActivity" />
        <!-- Contenuto dell'app -->
        <activity
            android:name=".view.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.MyPositionActivity"
            android:label="@string/title_activity_my_position" />
        <activity android:name=".view.RegistrationActivity" />
        <activity
            android:name=".view.TravelsActivity"
            android:label="@string/travels" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
            <meta-data android:name="android.support"
                android:value=".view.AddTravelActivity"></meta-data>
        </activity>
        <activity android:name=".view.TravelActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.TravelsActivity"></meta-data>
        </activity>
        <activity android:name=".view.NotesActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.NoteActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.NotesActivity"></meta-data>
        </activity>
        <activity android:name=".view.AddPictureActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.AddTravelActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.AddNoteActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.PicturesActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.PrivacyTermsActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
        <activity android:name=".view.AccountActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MenuActivity"></meta-data>
        </activity>
    </application>

</manifest>