<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddImageActivity">

    <!-- Nome che avrà l'immgine -->
    <EditText
        android:id="@+id/addImage_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Descrizione che avrà l'immagine -->
    <EditText
        android:id="@+id/addImage_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/addImage_name"/>

    <!-- Immagine -->
    <ImageView
        android:id="@+id/addImage_img"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@mipmap/ic_add_image"
        android:layout_below="@+id/addImage_description" />

    <!-- Scelgo la immagine-->
    <Button
        android:id="@+id/addImage_choose_image"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="165dp"
        android:gravity="center"
        android:title="Add Image" />

    <!-- Salvataggoi dei dati -->
    <Button
        android:id="@+id/addImage_save_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="92dp"
        android:title="Add Image" />
</RelativeLayout>